# Vision Package

## Package Details

Implements the perception stack and serves as the primary interface between image observations and downstream estimation and control modules. The package leverages differentiable rendering, segmentation, and feature extraction pipelines to generate object pose measurements.

## Primary Responsibilities

* AprilTag Pose Estimation
* ChArUco Board Pose Estimation
* Differentiable-Rendering-based Eye Pose Optimization
